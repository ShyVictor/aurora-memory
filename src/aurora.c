#include "../include/aurora.h"

bool server_container;

int main(int argc, char* argv[]){
    server_container = true;
    while (server_container)

    printf("Aurora in-memory database initialized\n");
    return 0;
}